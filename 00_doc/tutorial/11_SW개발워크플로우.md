# 11. SW 개발 워크플로우

> 신규 기능을 PRD·Plan에서 기능 단위 상세 문서(기획→설계→개발→검증)를 거쳐 커밋까지 완료하는 통합 워크플로우

## 문서 이력 관리
- v04 2026-04-10 — 보완 섹션 추가 — 코드 리뷰·통합 테스트·컨텍스트 재개·의존성·배포 누락 프로세스
- v03 2026-04-08 — plan 변경 시 대응 섹션 추가 — oofeature update --from-plan 워크플로우
- v02 2026-04-07 — ooflow 자동 실행 섹션 추가 — 완전 무인 자동화 기본 흐름 반영
- v01 2026-04-01 — 초기 작성

---

## 1. 개요

**이럴 때 사용:** 새 기능을 기획부터 커밋까지 체계적으로 완료할 때  
**결과물:** 구현 코드 + 테스트 + PRD/Plan/상세 문서 + Git 커밋

### 1.1. 실행 방식 선택

| 방식 | 명령어 | 특징 |
|------|--------|------|
| **자동 (권장)** | `ooflow run` | 완전 무인 자동화 — 사용자 확인 없이 끝까지 실행 |
| 단계별 확인 | `ooflow run --interactive` | 각 단계마다 사용자 승인 후 진행 |
| 특정 단계까지 | `ooflow run --until 검증` | 지정 단계까지만 실행 |
| 수동 | 아래 2~7절 개별 명령어 | 각 스킬을 직접 순서대로 실행 |

---

## 1.2. ooflow 자동 실행 (권장)

```bash
oostart run        # 세션 시작
oocontext 4        # SP 설정 (SP 번호는 상황에 맞게)
ooflow run         # 실행 계획 미리보기 → 확인 → 완전 무인 자동 실행
```

**ooflow run 실행 순서:**
```
[0] dry-run 자동 실행 → 실행 계획 간략 출력
        ↓
"위 계획으로 실행하시겠습니까? [y/N]"
        ↓ y
[1] PRD 확인/정합성 검증 (ooprd run)
[2] Plan 확인/갱신 (ooplan run)
[3] 미착수 Feature 목록 추출 (oofeature needed)
[4] Feature별 자동 반복 (이후 사용자 확인 없음):
    ├─ 상세기획 생성 (oofeature next)
    ├─ 기획→설계 전환 (oofeature next)
    ├─ 설계→개발 TDD (oodev run dXXXX)
    ├─ 개발→검증 체크 (oocheck run dXXXX)
    ├─ 이슈 자동 수정 (oofix run — CRITICAL/ERROR 발생 시)
    ├─ 완료 처리 (oofeature next)
    └─ Feature 커밋 (oocommit run)
[5] 문서 일괄 업데이트 (oodoc run)
[6] 이슈 아카이브 (oohistory run)
[7] 최종 커밋 (oocommit run)
```

**이슈 자동 처리 규칙:**

| 심각도 | 처리 |
|--------|------|
| CRITICAL | oofix 자동 실행 → 재검증 (최대 3회) |
| ERROR | oofix 자동 실행 → 재검증 1회 |
| WARNING | todo 등록 후 계속 진행 |

**강제 중단 조건:** CRITICAL 3회 반복 실패 | 컨텍스트 85% 초과 | 파일 시스템 오류

```bash
ooflow run --yes            # 확인 생략, 즉시 자동 실행
ooflow run --dry-run        # 실행 계획만 출력 (실행 안 함)
ooflow run --interactive    # 단계별 사용자 확인
ooflow run --feature d41001 # 특정 Feature 1개만 자동 처리
ooflow status               # 현재 SP 워크플로우 진행 현황
```

---

### 1.3. 전체 흐름 (수동)

```
oostart run → oocontext N → ooprd run → ooplan run
→ oofeature needed (미착수 Feature 확인)
→ [Feature별 반복]
    oofeature next dXXXX            ← 상세기획 (파일 없으면 자동 생성)
    oofeature next dXXXX            ← 기획→설계 전환 (설계 내용 작성)
    oodev run dXXXX                 ← 설계→개발 자동전환 + TC(RED) + GREEN + REFACTOR
    oocheck run dXXXX               ← 개발→검증 자동전환 + 코드 체크
    [oofix run]                     ← 이슈 수정 (필요시)
    oofeature next dXXXX            ← 완료 처리
    oocommit run
→ oodoc run → oohistory run → oocommit run → oostop run
```

---

## 2. 세션 시작 + 컨텍스트 설정

```bash
oostart run        # 환경 체크, 문서 상태 점검
oocontext 4        # SP04로 전환 (SP 번호는 상황에 맞게)
```

**확인 사항:**
- `[OK] PYTHONUTF8=1` 표시되는지
- 서브프로젝트 Active 상태 확인
- `d{SP}0004_todo.md`에 미해결 이슈가 있으면 먼저 처리 고려

---

## 3. 요구사항 정의 (PRD)

> **PRD 역할**: "무엇을(What)" 정의 — 3번 구현 계획에서 Feature 분해의 근거가 됨

```bash
ooprd run          # PRD 생성 → d{SP}0001_prd.md
```

**프로젝트 유형별 템플릿 선택:**

| 템플릿 | 유형 | 적용 상황 |
|--------|------|----------|
| common | 범용 | 일반 프로젝트 |
| streamlit | Type B | 웹 앱 (Streamlit) |
| algorithm | Type A | 알고리즘/분석/ML |
| agent | Type C | 에이전트/CLI 도구 |

```bash
ooprd run --template streamlit   # 템플릿 지정 생성
```

**ooplan Feature 분해를 위해 PRD에 반드시 포함해야 할 항목:**

| PRD 섹션 | 내용 | ooplan 활용 |
|----------|------|------------|
| §기능요구사항 | MoSCoW 기능 목록 | Epic → Feature 분해 근거 |
| §기능 상세 | 기능별 입출력/시나리오 | Feature → Task 세분화 근거 |
| §기술스택 | 언어/프레임워크 요약 | 기술 설계 방향 |

> PRD §기능요구사항이 불충분하면 ooplan이 Feature를 제대로 분해할 수 없음

**모호한 요구사항 해소 (Plan 전에 필수):**
```bash
ooprd clarify      # 최대 5개 질문으로 모호성 해소 → PRD 자동 업데이트
```

**이미 PRD가 있으면:**
```bash
ooprd run          # 기존 PRD 정합성 검증 (자동 분기)
ooprd validate     # 구조 검증 (필수 섹션 누락 여부)
```

---

## 4. 구현 계획

```bash
ooplan run         # PRD 기반 계획 생성 → d{SP}0002_plan.md
```

**Feature 목록 생성 기준 — PRD 자동 분해:**

```
PRD(무엇을) → Epic(도메인) → Feature(사용자 기능) → Task(구현 단위)
```

- PRD §기능요구사항이 Feature 목록의 근거
- `ooplan run` = PRD를 읽어 Epic > Feature > Task 자동 분해
- PRD §기능 상세가 충분해야 Feature 단위까지 분해 가능

**생성되는 내용:**
- Phase별 구현 순서 (Phase 1: 기반 → Phase 2: 핵심 → Phase 3: 고도화)
- 파일/모듈 목록 및 의존성
- Feature/Task 구조 (8.2절: 상세 문서 현황)

**Plan 완료 후 — 미착수 Feature 확인:**

```bash
oofeature needed   # plan.md Feature vs 상세 문서 교차 비교 → 미착수 목록 출력
```

```
[oofeature needed] SP04 — 상세기획 미착수 Feature

| Feature ID | 기능명          | Epic | 우선순위 | 권장 시작 단계 |
|-----------|----------------|------|:-------:|:------------|
| F001-1    | 데이터수집소스   | E001 | high    | 상세기획     |
| F001-2    | 크롤러 모듈 설계 | E001 | high    | 상세기획     |

총 2개 미착수 (전체 7개 중)
```

> 상세 문서(기획/설계/개발/검증 어떤 단계든)가 하나라도 존재하면 "착수"로 간주

---

## 5. 기능별 상세 문서 관리 [Feature 수만큼 반복]

### 5.1. 상세기획 문서 생성

```bash
oofeature next d41001
# → 파일 없으면 자동 생성 (plan.md에서 기능명 자동 조회)
# → 00_doc/sp04/d41001_상세기획_데이터수집소스.md 생성
```

> **`oofeature new` 사용 시점**: plan.md에 없는 기능을 추가하거나, 기능명을 직접 지정할 때만 사용
> ```bash
> oofeature new d41001 "데이터수집소스"   # 기능명 직접 지정
> ```

**생성 내용:** 기능 개요 / 요구사항 / 입출력 정의 / 제약조건 / 관련 Feature  
**자동:** plan.md 8.2절 갱신

```bash
oocommit run    # 기획 완료 후 커밋
```

### 5.2. 기획 → 설계 전환

```bash
oofeature next d41001
# → 파일명: 상세기획 → 상세설계
# → 설계 섹션 자동 추가 (아키텍처/API/DB)
```

```bash
oocommit run    # 설계 완료 후 커밋
```

### 5.3. 설계 → 개발 (TDD)

```bash
oodev run d41001
# → 설계 단계 감지 시 파일명 자동전환 (상세설계 → 상세개발)
# → TC 코드 작성 (RED — 반드시 실패해야 정상)
# → GREEN(최소 구현) → REFACTOR → VERIFY(런타임 검증)
```

> ⚠️ `oodev run dXXXX`가 TC 작성(RED)~REFACTOR까지 통합 수행. 별도 `ootest write` 불필요.

**`oodev run dXXXX` 동작:**
1. `00_doc/` 하위에서 `d41001_*.md` 자동 탐색
2. 상세설계 단계이면 → 파일명 rename (상세설계 → 상세개발) 자동 수행
3. 문서에서 Feature/Task 추출 (없으면 AI 자동 분해)
4. TC 코드 작성(RED) → GREEN(최소 구현) → REFACTOR → VERIFY(런타임 검증)
5. 완료 상태를 `d{SP}0003_test.md`에 기록

```bash
oocommit run    # 개발 완료 후 커밋
```

### 5.4. 개발 → 검증

```bash
oocheck run d41001
# → 개발 단계 감지 시 파일명 자동전환 (상세개발 → 상세검증)
# → [CRITICAL] 보안 취약점 (즉시 수정)
# → [ERROR] 로직 오류, 타입 불일치 (24시간 내)
# → [WARNING] 코드 품질, 성능 (1주일 내)
# → 이슈 발견 시 d{SP}0004_todo.md 자동 등록
```

**검증 실패 시:**
```bash
oofeature stage d41001 개발   # 개발 단계로 복귀
oodev run d41001               # 재개발
```

```bash
oocommit run    # 검증 통과 후 커밋
```

### 5.5. 완료 처리

```bash
oofeature next d41001
# → plan.md 8.2절에 ✅완료 표시
# → ooplan sync 자동 실행
```

---

## 단계 아이콘 & 롤백

| 단계 | 아이콘 | 롤백 명령 |
|------|:------:|-----------|
| 기획 | ⚪ | `oofeature stage dXXXX 기획` |
| 설계 | 🔵 | `oofeature stage dXXXX 설계` |
| 개발 | 🟡 | `oofeature stage dXXXX 개발` |
| 검증 | 🟢 | `oofeature stage dXXXX 검증` |
| 완료 | ✅ | — |

---

## Plan 변경 시 대응

상세개발·검증 진행 중 `plan.md`가 수정된 경우:

```bash
oofeature update --from-plan                    # 영향 문서 분석 (롤백 후보 표시)
oofeature update --from-plan --apply            # 실제 롤백 실행 (이전 단계로 rename)
oofeature update --from-plan --apply --dry-run  # 미리보기 (실제 수정 안 함)
```

**동작 방식:**
1. `git diff HEAD` 기반으로 plan.md 변경 감지
2. 변경된 줄에서 doc 번호(`d41001` 등) 추출
3. 해당 번호의 상세 문서(설계/개발/검증 단계) 식별
4. 이전 단계로 rollback 제안 (개발→설계, 검증→개발 등)

**diff 없는 경우:** plan.md 미수정으로 감지되면 전체 활성(설계/개발/검증) 문서 목록 표시

| 옵션 | 설명 |
|------|------|
| (없음) | 영향 문서 분석 + 롤백 후보 출력만 |
| `--apply` | 실제 파일 rename (이전 단계로 롤백) |
| `--apply --dry-run` | rename 미리보기 (실제 수정 안 함) |
| `--sp N` | 특정 SP만 분석 |

---

## 현황 조회

```bash
oofeature list
```

**출력 예시:**
```
[oofeature list] SP04
| 문서번호 | 기능명        | 단계   | 파일                              |
|---------|--------------|--------|-----------------------------------|
| d41001  | 데이터수집소스 | 🔵설계 | d41001_상세설계_데이터수집소스.md |
| d40101  | 크롤러모듈    | 🟡개발 | d40101_상세개발_크롤러모듈.md     |
총 2개 | 기획:0 설계:1 개발:1 검증:0 완료:0
```

---

## 6. 문서 현행화 + 최종 커밋

```bash
oodoc run          # d{SP}0001~d{SP}0010 일괄 업데이트
oohistory run      # 완료된 이슈 → d{SP}0010_history.md 아카이브
oocommit run       # Git 커밋 (feat/fix/docs 자동 분류)
```

**커밋 메시지 형식:**
```
feat(SP04): 새 기능 구현 내용 요약
fix(SP04): 버그 수정 내용
docs(SP04): 문서 업데이트
```

---

## 7. 세션 종료

```bash
oostop run
```

**체크 항목:**
- 미커밋 변경사항 있는지
- 미해결 이슈 잔여 건수
- 다음 세션 작업 안내

---

## 파일명 규칙

```
d{SP번호}{기능번호}_상세{단계}_{기능명}.md
예) d41001_상세설계_데이터수집소스.md
```

> 상세 규칙 → **[33_문서번호및파일명규칙.md](33_문서번호및파일명규칙.md)** 참조

---

## 소요 시간 가이드

| 단계 | 소요 시간 | 비고 |
|------|----------|------|
| 세션 시작 | ~1분 | 자동 |
| PRD + Plan | ~5분 | 요구사항 복잡도에 따라 |
| 기능별 기획/설계 | ~3분/기능 | 자동 |
| 테스트 작성 | ~10분 | 기능 수에 따라 |
| 구현 | 가변 | 핵심 작업 |
| 품질 체크+수정 | ~5분 | 자동 |
| 문서+커밋 | ~3분 | 자동 |

---

> 관련: [12_TDD상세.md](00_doc/tutorial/12_TDD상세.md) | [13_버그수정흐름.md](00_doc/tutorial/13_버그수정흐름.md) | [16_개발테스트.md](00_doc/tutorial/16_개발테스트.md) | [29_Git커밋이력정리.md](00_doc/tutorial/29_Git커밋이력정리.md) | [37_문서번호및파일명규칙.md](00_doc/tutorial/37_문서번호및파일명규칙.md)

---

## 보완: 워크플로우에서 빠진 프로세스

> 위 1~7절 워크플로우에 포함되지 않은 실무 상황별 대응 가이드

---

### B1. 코드 리뷰 (ooreview)

현재 워크플로우에서 `oocheck`는 자동화된 정적 분석이지만, **AI 코드 리뷰**(설계 의도·가독성·보안 종합)는 별도 단계입니다.

**삽입 시점:** `oocheck run` 통과 후, `oofeature next`(완료 처리) 전

```bash
oocheck run d41001      # 자동 정적 분석
ooreview run d41001     # AI 코드 리뷰 (code-reviewer + security + quality 병렬)
oofeature next d41001   # 완료 처리
oocommit run
```

**ooreview 활용 기준:**

| 상황 | 적용 여부 |
|------|----------|
| 보안 관련 기능 (인증, 파일 I/O) | 필수 |
| 외부 API 연동 | 권장 |
| 단순 유틸 함수 | 선택 |

---

### B2. 통합 테스트 (여러 Feature 완료 후)

개별 Feature 검증(oocheck)은 단위 수준이며, **여러 Feature 완료 후 전체 통합 테스트**가 필요합니다.

**삽입 시점:** Phase 내 모든 Feature 완료 후, `oodoc run` 전

```bash
# Phase 1 Feature 전부 완료 후
ootest run              # 전체 TC 실행 + 커버리지 확인
ootest coverage         # 커버리지 리포트 (목표: 80%+)

# 실패 시
oofix run               # 회귀 오류 수정
ootest run              # 재실행 확인
```

**회귀 방지 체크 (Feature 추가할 때마다):**
```bash
ootest run --regression   # 기존 TC가 새 구현으로 깨지지 않는지 확인
```

---

### B3. 컨텍스트 초과 시 세션 재개

`ooflow run` 강제 중단 조건(컨텍스트 85% 초과) 발생 후 재개 방법입니다.

```bash
# 1. 현재 진행 상태 확인
oofeature list          # 어떤 Feature가 어느 단계인지 확인
ooflow status           # 워크플로우 진행 현황

# 2. 새 세션에서 재개
oostart run
oocontext N             # 동일 SP 번호로 재진입
ooflow run --feature d41002   # 중단된 Feature부터 재개
```

**컨텍스트 절감 팁:**
```bash
ootoken                 # 현재 토큰 사용량 확인
# → 70% 이상이면 ooflow --feature 단위로 나눠 실행
```

---

### B4. Feature 간 의존성 처리

Feature A가 Feature B의 코드에 의존할 때 순서 관리가 필요합니다.

**원칙:** `plan.md`의 Phase 순서 = 구현 순서 (낮은 Phase 먼저)

**의존 관계 확인:**
```bash
oofeature list          # 현재 단계 확인
# plan.md 8.2절에서 의존 관계 수동 확인
```

**의존 Feature가 완료되지 않은 경우:**
```bash
# 의존 Feature 먼저 처리
oofeature next d41001   # 선행 Feature 완료
oocommit run

# 그 다음 의존하는 Feature 진행
oofeature next d41002
oodev run d41002
```

> `oodev run d41002` 실행 전 선행 Feature 코드가 없으면 TC가 import 오류로 실패함 — 반드시 선행 완료 후 진행

---

### B5. 패키지 의존성 추가 (oouv)

개발 중 새 외부 패키지가 필요한 경우 `pyproject.toml` 갱신이 워크플로우에 포함되지 않습니다.

**삽입 시점:** `oodev run` 중 import 오류 발생 시

```bash
# 패키지 추가
oouv add requests           # pyproject.toml에 추가 + uv sync

# 추가 후 반드시
ooenv uv check              # 충돌/취약점 확인
oocommit run                # pyproject.toml, uv.lock 함께 커밋
```

**패키지 추가 커밋 메시지 형식:**
```
chore(deps): requests 추가 — SP04 웹 크롤링 기능
```

---

### B6. 실행 확인 (배포/동작 검증)

구현이 완료됐더라도 **실제 실행 동작**을 확인하는 단계가 없습니다.

**삽입 시점:** `ootest run` 통과 후, 최종 `oocommit run` 전

```bash
oorun run               # 프로젝트 실행 (설정된 실행 방법으로)
# → 예: uv run python main.py, streamlit run app.py 등

# 실행 오류 발생 시
oocheck run             # 런타임 에러 재분석
oofix run               # 수정
oorun run               # 재확인
```

**SP 유형별 실행 확인:**

| SP 유형 | 확인 방법 |
|---------|----------|
| Streamlit 앱 | `streamlit run app.py` → UI 동작 확인 |
| Python 스크립트 | `uv run python main.py` → stdout/stderr 확인 |
| API 서버 | `uvicorn main:app` → 주요 엔드포인트 curl 테스트 |
| CLI 도구 | `uv run python cli.py --help` → 주요 명령어 실행 |

---

### B7. 보완 적용 위치 요약

```
[기존 워크플로우]                    [보완 삽입 위치]

oodev run dXXXX          ─────────────────────────────
                                  ↓ B5. 패키지 추가 (필요시)
oocheck run dXXXX        ─────────────────────────────
                                  ↓ B1. ooreview (보안/외부 연동 시)
oofeature next dXXXX     ─────────────────────────────
oocommit run

[Phase 내 모든 Feature 완료]
                                  ↓ B4. Feature 간 의존성 확인
                                  ↓ B2. ootest run (통합 테스트)
                                  ↓ B3. 컨텍스트 초과 시 재개 (필요시)
                                  ↓ B6. oorun (실행 확인)
oodoc run
oohistory run
oocommit run
```
