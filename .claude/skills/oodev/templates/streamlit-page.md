# d{문서번호}: {페이지명}

## 문서 정보

| 항목 | 내용 |
|------|------|
| 문서번호 | d{문서번호} |
| 페이지 ID | {대분류번호}_{페이지번호} |
| 페이지명 | {페이지명} |
| 대분류 | {대분류} |
| 상위 PRD | d{PRD번호}_prd.md |
| 작성일 | YYYY-MM-DD |

## 문서 이력 관리
- v01 YYYY-MM-DD — 최초 생성

---

## 1. 개요

### 1.1 목적

{페이지의 목적을 간략히 기술}

- **{핵심기능1}**: {설명}
- **{핵심기능2}**: {설명}
- **{핵심기능3}**: {설명}

### 1.2 데이터 소스

- {데이터 소스 1}
- {데이터 소스 2}

### 1.3 처리 방식

- {처리 방식 1 (크롤링/API/수동 입력 등)}
- {처리 방식 2}

### 1.4 주요 필드

- {필드1}: {설명}
- {필드2}: {설명}
- {필드3}: {설명}

### 1.5 중복 기준

- {중복 판정 기준 (URL/해시/복합키 등)}

### 1.6 주요 처리

{핵심 처리 로직 요약. 호출 함수, 실행 방식(수동/자동), 결과 표시 방법}

- **수동 실행 (UI 버튼)**
  - {실행 버튼 설명}
  - {진행 상태 표시 방법}
  - {완료 후 결과 요약}

- 자동 실행 (cron/배치) (해당시)
  - {스케줄 설명}
  - {실행 결과 기록 방법}

- 파라미터 설정 (해당시)
  - {파라미터1}: {설명}
  - {파라미터2}: {설명}

### 1.7 데이터 조회 및 편집

{데이터 조회/편집 UI의 핵심 패턴 설명}

#### 1.7.1 데이터 조회 테이블

- **필터/검색 영역**
  - {필터1}: {설명}
  - {필터2}: {설명}

- 테이블 (`selection_mode="{single-row|multi-row}"`)
  - 표시 컬럼: {컬럼 목록}
  - 정렬: {정렬 기준}

- 통계 (해당시)
  - {통계 항목 설명}

#### 1.7.2 단건 선택 → 상세 편집 UI

{1건 선택 시 표시되는 상세 편집 UI 설명}

- **편집 영역 구성**
  - {필드1}: `st.{컴포넌트}` - {설명}
  - {필드2}: `st.{컴포넌트}` - {설명}

- **저장 버튼**
  - {저장 시 동작 설명}

#### 1.7.3 복수 선택 → 일괄 작업 UI (해당시)

{2건 이상 선택 시 표시되는 일괄 작업 UI 설명}

- **일괄 수정 탭**: {설명}
- **일괄 삭제 탭**: {설명}
- **일괄 {기타} 탭**: {설명}

#### 1.7.4 UI 분기 로직 요약

```
선택된 행 수 판별:
├── 0건: {동작}
├── 1건: {동작}
└── 2건 이상: {동작}
```

### 1.8 DB 테이블

| 용도 | 테이블명 | 설명 |
|------|---------|------|
| {용도} | {테이블명} | {설명} |

> {DB 구조 관련 보충 설명}

---

## 2. 페이지 목차

| 번호 | 섹션명 | 설명 |
|------|--------|------|
| 1 | {섹션명} | {설명} |
| 2 | {섹션명} | {설명} |
| 3 | {섹션명} | {설명} |
| 4 | {섹션명} | {설명} |
| 5 | {섹션명} | {설명} |
| 6 | {섹션명} | {설명} |

---

## 3. UI 구성

### 3.1 페이지 레이아웃

```
┌─────────────────────────────────────────────────────┐
│ {섹션1: 설정/실행 영역}                                │
├─────────────────────────────────────────────────────┤
│ {섹션2: 통계 영역}                                    │
├─────────────────────────────────────────────────────┤
│ {섹션3: 필터/검색 영역}                                │
├─────────────────────────────────────────────────────┤
│ {섹션4: 데이터 테이블}                                 │
├─────────────────────────────────────────────────────┤
│ {섹션5: 상세 편집 (단건 선택 시)}                       │
├─────────────────────────────────────────────────────┤
│ {섹션6: 일괄 편집 (복수 선택 시)}                       │
└─────────────────────────────────────────────────────┘
```

### 3.2 컴포넌트 참조

#### 3.2.1 {영역1} 영역

| UI 요소 | Streamlit 컴포넌트 | key | 설명 |
|---------|-------------------|-----|------|
| {요소명} | `st.{컴포넌트}` | {key} | {설명} |
| {요소명} | `st.{컴포넌트}` | {key} | {설명} |

#### 3.2.2 {영역2} 영역

| UI 요소 | Streamlit 컴포넌트 | key | 설명 |
|---------|-------------------|-----|------|
| {요소명} | `st.{컴포넌트}` | {key} | {설명} |
| {요소명} | `st.{컴포넌트}` | {key} | {설명} |

#### 3.2.3 {영역3} 영역

| UI 요소 | Streamlit 컴포넌트 | key | 설명 |
|---------|-------------------|-----|------|
| {요소명} | `st.{컴포넌트}` | {key} | {설명} |

#### 3.2.4 데이터 테이블

| UI 요소 | Streamlit 컴포넌트 | 설명 |
|---------|-------------------|------|
| 데이터 테이블 | `st.dataframe(selection_mode="{mode}", on_select="rerun")` | {설명} |
| 빈 결과 안내 | `st.info("조회 결과가 없습니다.")` | df.empty 시 st.stop() |

표시 컬럼: {컬럼 목록}

#### 3.2.5 단건 상세 편집 UI (1건 선택 시)

**{서브섹션1}**

| UI 요소 | Streamlit 컴포넌트 | key | 설명 |
|---------|-------------------|-----|------|
| {요소명} | `st.{컴포넌트}` | {key} | {설명} |

**{서브섹션2}**

| UI 요소 | Streamlit 컴포넌트 | key | 설명 |
|---------|-------------------|-----|------|
| {요소명} | `st.{컴포넌트}` | {key} | {설명} |

**저장**

| UI 요소 | Streamlit 컴포넌트 | key | 설명 |
|---------|-------------------|-----|------|
| 저장 버튼 | `st.button("{버튼텍스트}", type="primary")` | btn_save | {설명} |
| 저장 완료 | `st.success` | - | {설명} |

#### 3.2.6 일괄 편집 UI (2건 이상 선택 시) (해당시)

| UI 요소 | Streamlit 컴포넌트 | key | 설명 |
|---------|-------------------|-----|------|
| 작업 탭 | `st.tabs(["{탭1}", "{탭2}", "{탭3}"])` | - | {설명} |

**{탭1} 탭:**

| UI 요소 | Streamlit 컴포넌트 | key | 설명 |
|---------|-------------------|-----|------|
| {요소명} | `st.{컴포넌트}` | {key} | {설명} |

**{탭2} 탭:**

| UI 요소 | Streamlit 컴포넌트 | key | 설명 |
|---------|-------------------|-----|------|
| {요소명} | `st.{컴포넌트}` | {key} | {설명} |

---

## 4. 데이터 모델

### 4.1 테이블 스키마

**테이블명**: `{테이블명}`

| 컬럼명 | 타입 | 기본값 | 설명 |
|--------|------|--------|------|
| id | INTEGER | PK (자동증가) | ID |
| {컬럼명} | {타입} | {기본값} | {설명} |
| created_at | TIMESTAMP | CURRENT_TIMESTAMP | 생성일시 |
| updated_at | TIMESTAMP | NULL | 수정일시 |

> {데이터 모델 설계 근거 또는 보충 설명}

### 4.2 데이터 흐름

```
[{단계1}] {설명} → {테이블} INSERT ({초기 상태})
          │
          ▼
[{단계2}] {설명} → {상태 변경}
          │
          ▼
[{단계3}] {설명} → {최종 조회 조건}
```

---

## 5. 비즈니스 로직

### 5.1 {핵심 처리 흐름 1}

```
[{트리거}]
    │
    ▼
{함수명}({파라미터})
    │
    ├── 1. {단계1}
    ├── 2. {단계2}
    ├── 3. {단계3}
    └── 4. {단계4}
    │
    ▼
결과 반환: {{결과 필드}}
```

### 5.2 {핵심 처리 흐름 2}

```
{시작 조건}
    │
    ▼
{처리 단계}
    │
    ▼
{DB 반영}: UPDATE {테이블} SET {필드}=? WHERE id=?
```

### 5.3 {핵심 처리 흐름 3} (해당시)

```
{흐름 설명}
```

### 5.4 {특수 로직} (해당시)

#### 5.4.1 {로직 상세 1}

```python
# {로직 설명}
if {조건}:
    {처리}
else:
    {대체 처리}
```

#### 5.4.2 {로직 상세 2}

{운영 시나리오 및 기본값 규칙 설명}

---

## 6. API/외부 연동

### 6.1 oo 모듈 의존성

| 모듈 | 함수 | 용도 |
|------|------|------|
| `oo.{모듈명}` | `{함수명}()` | {용도} |
| `oo.config_helper` | `Config.DB_PATH` | DB 경로 참조 |

### 6.2 외부 라이브러리

| 라이브러리 | 용도 |
|-----------|------|
| {라이브러리1} | {용도} |
| {라이브러리2} | {용도} |

### 6.3 외부 데이터 소스 (해당시)

| 소스명 | URL/경로 |
|--------|---------|
| {소스1} | {URL} |
| {소스2} | {URL} |

---

## 7. 플랜

> **작성 시점**: 구현 착수 전 작성

### 7.1 구현 개요

#### 7.1.1 목표 요약

{구현 목표를 간략히 기술}

#### 7.1.2 기술적 접근법

| 영역 | 접근법 | 근거 |
|------|--------|------|
| 프론트엔드 | Streamlit | 기존 페이지 일관성 |
| 데이터베이스 | SQLite (sene.sqlite) | 프로젝트 표준 |
| 파일 저장 | file/{기능명}/ | 기존 패턴 준수 |

### 7.2 작업 분해 (WBS)

#### 7.2.1 Phase/Task 목록

| Phase | Task ID | Task 명 | 복잡도 | 우선순위 | 의존성 |
|-------|---------|---------|--------|---------|--------|
| **Phase 1: 문서 정합성** | T001 | PRD 섹션 추가 | 낮음 | Critical | - |
| | T002 | DB 스키마 문서 등록 | 중간 | Critical | - |
| **Phase 2: DB 구축** | T003 | 테이블 생성 스크립트 작성 | 중간 | Critical | T002 |
| | T004 | 테이블 생성 실행 | 낮음 | Critical | T003 |
| **Phase 3: 구현** | T005 | 페이지 파일 생성 | 높음 | High | T004 |
| | T006 | 핵심 로직 구현 | 높음 | High | T005 |
| **Phase 4: 검증** | T007 | 기능 테스트 | 중간 | High | T006 |
| | T008 | 문서 현행화 | 낮음 | Medium | T007 |

#### 7.2.2 작업 의존성

```
Phase 1 (문서)
    T001 ──┐
    T002 ──┴──> Phase 2 (DB)
                    T003 ──> T004 ──> Phase 3 (구현)
                                          T005 ──> T006 ──> Phase 4
                                                              T007 ──> T008
```

### 7.3 상세 태스크 정의

#### T001: PRD 섹션 추가

- **작업**: d0001_prd.md에 페이지 요구사항 추가
- **파일**: 00_doc/sp00/d0001_prd.md
- **예상시간**: 15분

#### T002: DB 스키마 문서 등록

- **작업**: d0006_db.md에 신규 테이블 스키마 등록
- **파일**: 00_doc/sp00/d0006_db.md
- **참조**: 본 문서 4.1 섹션
- **예상시간**: 30분

{나머지 태스크 상세 정의...}

### 7.4 실행 순서

1. **Phase 1: 문서 정합성** - 병렬 실행 가능
2. **Phase 2: DB 구축** - 순차 실행
3. **Phase 3: 구현** - 순차 실행
4. **Phase 4: 검증** - 순차 실행

### 7.5 리스크 관리

| ID | 리스크 | 대응 전략 |
|----|--------|----------|
| R001 | {리스크1} | {대응 전략} |
| R002 | {리스크2} | {대응 전략} |

### 7.6 완료 기준 (DoD)

- [ ] 정합성 점수 100% 달성
- [ ] 모든 테스트 통과
- [ ] 문서 버전 업데이트
- [ ] d0010_history.md 이력 기록

---

## 8. 구현

> **작성 시점**: 구현 완료 후 작성

### 8.1 구현 결과

| 항목 | 내용 |
|------|------|
| 구현 파일 | `02_1st_server/pages/{N}_{NN}_{페이지명}.py` |
| 코드 라인 수 | {라인수} |
| 구현 완료일 | YYYY-MM-DD |

### 8.2 주요 구현 사항

- {구현 사항 1}
- {구현 사항 2}
- {구현 사항 3}

### 8.3 변경/추가된 파일 목록

| 파일 | 변경 유형 | 설명 |
|------|----------|------|
| `pages/{페이지}.py` | 신규 | 메인 페이지 |
| `00_doc/sp00/d0006_db.md` | 수정 | 테이블 스키마 추가 |

---

## 9. 검증

> **작성 시점**: 구현 완료 후 검증 수행

### 9.1 검증 체크리스트

#### 9.1.1 oo 모듈 테스트

| 검증 항목 | 결과 | 비고 |
|----------|------|------|
| `check_login_status()` 정상 동작 | PASS/FAIL | |
| `get_user_info()` 정상 반환 | PASS/FAIL | |
| `exec_r_query()` 쿼리 실행 | PASS/FAIL | |
| `exec_cud_query()` 데이터 저장 | PASS/FAIL | |
| `dt_str_gen()` 날짜 형식 | PASS/FAIL | |

#### 9.1.2 UI 테스트

| 검증 항목 | 결과 | 비고 |
|----------|------|------|
| 페이지 정상 로딩 | PASS/FAIL | |
| 사이드바 렌더링 | PASS/FAIL | |
| 폼 입력 필드 동작 | PASS/FAIL | |
| 버튼 클릭 이벤트 | PASS/FAIL | |
| 에러 메시지 표시 | PASS/FAIL | |
| 성공 메시지 표시 | PASS/FAIL | |

#### 9.1.3 데이터 테스트

| 검증 항목 | 결과 | 비고 |
|----------|------|------|
| 데이터 저장 정상 | PASS/FAIL | |
| 데이터 조회 정상 | PASS/FAIL | |
| 데이터 수정 정상 | PASS/FAIL | |
| 데이터 삭제 정상 | PASS/FAIL | (해당시) |
| FK 제약조건 준수 | PASS/FAIL | |

#### 9.1.4 시나리오 테스트

| 시나리오 | 결과 | 비고 |
|----------|------|------|
| 정상 플로우 완료 | PASS/FAIL | |
| 필수 입력 누락 시 | PASS/FAIL | |
| 비로그인 상태 접근 | PASS/FAIL | |
| 권한 없는 사용자 접근 | PASS/FAIL | (해당시) |

### 9.2 발견된 이슈

| 이슈 ID | 내용 | 심각도 | 상태 | 등록처 |
|---------|------|--------|------|--------|
| {이슈1} | {내용} | Critical/High/Medium/Low | Open/Resolved | d0004_todo.md |

### 9.3 검증 결과 요약

| 항목 | 결과 |
|------|------|
| oo 모듈 테스트 | PASS/FAIL |
| UI 테스트 | PASS/FAIL |
| 데이터 테스트 | PASS/FAIL |
| 시나리오 테스트 | PASS/FAIL |
| **최종 결과** | **PASS/FAIL** |

### 9.4 검증 완료 확인

- [ ] 모든 테스트 항목 PASS
- [ ] 발견된 이슈 모두 해결 또는 d0004_todo.md 등록
- [ ] 검증 결과 d0003_test.md에 기록

---

## 10. 관련 문서

- `00_doc/d{PRD번호}_prd.md`: 프로젝트 요구사항
- `00_doc/sp00/d0006_db.md`: 데이터베이스 스키마
- `00_doc/sp00/d0003_test.md`: 테스트 결과 기록
- `00_doc/sp00/d0010_history.md`: 변경 이력
