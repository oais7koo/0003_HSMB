#!/usr/bin/env python3
"""oo 스킬 전체에 ## 0. 스킬 요약 (호환성 섹션) 삽입 스크립트"""
import re, os

BASE = os.path.join(os.path.dirname(__file__))

SECTIONS = {
"ooprd": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | PRD(요구사항 정의서) 생성 및 코드·문서 정합성 검증 |
| **하는 것** | d{SP}0001_prd.md 신규 생성, 현행화, PRD↔코드 정합성 검증 |
| **하지 않는 것** | 구현 계획 수립(→ooplan), 코드 구현(→oodev), 이슈 수정(→oofix) |
| **참조 범위** | 현재 프로젝트 내부 파일만 (코드, 기존 문서) / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `00_doc/sp{N}/d{SP}0001_prd.md` |
| **실행 레벨** | [반자동] — 생성 계획 표시 → 확인 후 실행 |
| **에이전트 호환** | 범용 — Claude Code에서 Explore·task-checker 서브에이전트 자동 활용 |

""",
"ooplan": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | PRD 기반 구현 계획(Epic/Feature/Task) 수립 |
| **하는 것** | d{SP}0002_plan.md 생성/갱신, 아키텍처·API·DB·워크플로우 설계 |
| **하지 않는 것** | PRD 작성(→ooprd), 코드 구현(→oodev), 이슈 수정(→oofix) |
| **참조 범위** | 현재 프로젝트 내부 파일만 (PRD, 코드 구조) / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `00_doc/sp{N}/d{SP}0002_plan.md` |
| **실행 레벨** | [반자동] — 계획 구조 확인 후 실행 |
| **에이전트 호환** | 범용 — Claude Code에서 Explore·task-executor 서브에이전트 자동 활용 |

""",
"oodev": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | TDD GREEN 단계 구현 — 테스트 통과하는 최소 코드 작성 + REFACTOR |
| **하는 것** | d{SP}0003_test.md 초기 생성(INIT), GREEN 구현, REFACTOR |
| **하지 않는 것** | TC 코드 작성(→ootest/RED), 정적 분석(→oocheck), 이슈 등록(→oocheck) |
| **참조 범위** | 현재 프로젝트 내부 파일만 (plan.md, test.md, 코드) / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | 구현 코드 파일, `d{SP}0003_test.md` |
| **실행 레벨** | [자동] — explore→executor→reviewer 서브에이전트 순서 자동 위임 |
| **에이전트 호환** | Claude Code 권장 — Agent 도구 필수 / 다른 에이전트: 서브에이전트 위임 단계를 수동으로 실행 |

""",
"ootest": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | TDD RED 단계 — TC 코드 작성 및 테스트 반복 실행 (PASS까지) |
| **하는 것** | tests/ TC 파일 생성, d{SP}0003 Part C 등록, pytest 반복 실행 |
| **하지 않는 것** | 구현 코드 작성(→oodev/GREEN), 정적 분석(→oocheck), 이슈 수정(→oofix) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `tests/TC*.py`, `d{SP}0003_test.md` Part C |
| **실행 레벨** | [자동] — TC 작성 후 pytest 반복 실행 (PASS까지) |
| **에이전트 호환** | Claude Code 권장 — `uv run pytest` 직접 실행 / 다른 에이전트: pytest 명령을 수동 실행하거나 별도 터미널에서 실행 |

""",
"oocheck": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 코드 정적 분석 및 품질 체크 (Python/Flutter 자동 감지) |
| **하는 것** | py_compile→pylint→mypy→pytest 검증 체인 실행, 이슈 d{SP}0004 등록 |
| **하지 않는 것** | 이슈 수정(→oofix), 설계 검토(→ooreview), 실행 테스트(→ootest) |
| **참조 범위** | 현재 프로젝트 내부 파일만 (src/, oo/, tests/, lib/) / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `d{SP}0004_todo.md` (이슈 등록만, 코드 수정 안 함) |
| **실행 레벨** | [자동] — 스캔→분석→등록 자동 실행 |
| **에이전트 호환** | Claude Code 권장 — `uv run` 기반 도구 체인 자동 실행 / 다른 에이전트: pylint·mypy·pytest를 수동 실행 후 결과를 d{SP}0004에 직접 기록 |

""",
"oofix": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | d{SP}0004_todo.md 활성 이슈를 서브에이전트 병렬 처리로 자동 수정 |
| **하는 것** | 코드 이슈(S/T/W) 자동 수정, 수정 후 d{SP}0010으로 이동 |
| **하지 않는 것** | 커스텀 Todo 처리(→ootodo), 이슈 발견(→oocheck), Git 커밋(→oocommit) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | 이슈 관련 코드 파일, `d{SP}0004_todo.md`, `d{SP}0010_history.md` |
| **실행 레벨** | [자동] — 3단계(분석→병렬수정→검증) 자동 실행 |
| **에이전트 호환** | Claude Code 권장 — 병렬 서브에이전트 자동 배치 / 다른 에이전트: 이슈별 순차 처리로 대체 가능 |

""",
"oocommit": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | Git 커밋 실행 + d{SP}0004 완료 항목 → d{SP}0010 이력 이동 통합 |
| **하는 것** | git commit, d{SP}0004 완료 항목 아카이브, GitHub Issues 연동(선택) |
| **하지 않는 것** | 코드 수정(→oofix), 이슈 발견(→oocheck), git push(별도 확인 필요) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 저장소 자동 push 안 함 |
| **수정 대상** | Git 커밋, `d{SP}0004_todo.md`, `d{SP}0010_history.md` |
| **실행 레벨** | [반자동] — `--dry-run` 권장, 확인 후 실행 |
| **에이전트 호환** | 범용 — `git` CLI 사용 / GitHub Issues 연동(`github` 서브명령)은 `gh` CLI 및 MCP 필요 |

""",
"oofeature": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 기능별 상세 문서를 기획→설계→개발→검증 4단계로 생명주기 관리 |
| **하는 것** | 상세기획/설계/개발/검증 문서 생성, 파일명 rename으로 단계 전환, 미착수 Feature 리스트업 |
| **하지 않는 것** | 코드 구현(→oodev), 테스트 실행(→ootest), 코드 체크(→oocheck) |
| **참조 범위** | 현재 프로젝트 내부 파일만 (`00_doc/sp{N}/dXXXX_*.md`) / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `00_doc/sp{N}/dXXXX_상세*.md` (파일명 rename 포함) |
| **실행 레벨** | [반자동] — 단계 전환 전 현재 단계 확인 후 실행 |
| **에이전트 호환** | 범용 — 파일명 rename은 파일 시스템 작업으로 모든 에이전트 처리 가능 |

""",
"ooflow": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | SW 개발 전체 워크플로우(기획→설계→개발→검증→커밋)를 단일 명령으로 오케스트레이션 |
| **하는 것** | oofeature·oodev·ootest·oocheck·ooreview·oofix·oodoc·oocommit 순차 위임 |
| **하지 않는 것** | 각 단계의 직접 구현(스킬 위임만), git push(별도 실행) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | 각 위임 스킬의 산출물 전체 (코드, 00_doc/, git) |
| **실행 레벨** | [반자동] — dry-run 표시 → 확인 1회 → 완전 자동 (`--yes`로 즉시 자동) |
| **에이전트 호환** | Claude Code 권장 — 스킬 간 자동 위임 활용 / 다른 에이전트: 각 스킬을 단계별 수동 실행 |

""",
"ooreview": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 설계·패턴·보안·성능 판단 기반 AI 코드 리뷰 (oocheck 정적 분석과 다름) |
| **하는 것** | code-reviewer·security-reviewer·quality-reviewer 병렬 리뷰, 이슈 d{SP}0004 등록 |
| **하지 않는 것** | 정적 분석(→oocheck), 이슈 수정(→oofix), 코드 구현(→oodev) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `d{SP}0004_todo.md` (리뷰 이슈 등록만, 코드 수정 안 함) |
| **실행 레벨** | [자동] — 병렬 리뷰 에이전트 자동 실행 |
| **에이전트 호환** | Claude Code 권장 — opus 모델 병렬 에이전트 자동 배치 / 다른 에이전트: 리뷰 항목별 순차 처리로 대체 |

""",
"oodoc": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | d0001~d0010 핵심 문서 생성·업데이트·최적화 오케스트레이터 |
| **하는 것** | PRD·Plan·Test·Todo·History·User·Env 문서 일괄 생성/현행화, 문서 정합성 검사 |
| **하지 않는 것** | 코드 수정(→oodev), 이슈 수정(→oofix), 스킬 파일 수정(→ooskill) |
| **참조 범위** | 현재 프로젝트 내부 파일만 (`00_doc/`, `.claude/skills/`) / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `00_doc/sp{N}/d{SP}0001~d{SP}0010_*.md` |
| **실행 레벨** | [자동] — 대상 문서 자동 탐지 후 일괄 처리 |
| **에이전트 호환** | 범용 — 파일 읽기·쓰기 작업 중심으로 모든 에이전트 처리 가능 |

""",
"ooenv": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 개발 환경(플러그인·Python 의존성·스킬 정합성) 일괄 점검 및 수정 |
| **하는 것** | uv 의존성 확인, 플러그인 상태 점검, d0009_env.md 현행화, 스킬 alias 관리 |
| **하지 않는 것** | 코드 품질 체크(→oocheck), 패키지 업데이트(→oouv) |
| **참조 범위** | 현재 프로젝트 + 로컬 환경 설정 파일 / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `00_doc/sp00/d0009_env.md` |
| **실행 레벨** | [자동] — 환경 스캔 후 d0009 자동 생성/갱신 |
| **에이전트 호환** | Claude Code 권장 — `uv run` 스크립트 및 플러그인 시스템 사용 / 다른 에이전트: uv 명령 수동 실행 후 결과를 d0009에 직접 기록 |

""",
"oohistory": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | d{SP}0004_todo.md 완료 항목을 d{SP}0010_history.md로 이동 (아카이브) |
| **하는 것** | 완료 이슈 이동, 오래된 이력 자동 축약(>20K→10K), SP 병행 처리 |
| **하지 않는 것** | Git 커밋(→oocommit), 이슈 수정(→oofix), 커스텀 Todo 처리(→ootodo) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `d{SP}0004_todo.md`, `d{SP}0010_history.md` |
| **실행 레벨** | [자동] — 완료 항목 자동 탐지 후 이동 |
| **에이전트 호환** | 범용 — 파일 읽기·쓰기 작업 중심으로 모든 에이전트 처리 가능 |

""",
"ootodo": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | d{SP}0004_todo.md "커스텀 Todo" 섹션 관리 및 할 일 자동 처리 |
| **하는 것** | Todo 추가·즉시처리·일괄처리·상태조회·아카이브 |
| **하지 않는 것** | 코드 에러 이슈 처리(→oofix), 이슈 발견(→oocheck), 이력 아카이브(→oohistory) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `d{SP}0004_todo.md` (커스텀 Todo 섹션만), `d{SP}0010_history.md` |
| **실행 레벨** | [반자동] — 코딩 작업은 oodev 위임, 비코딩 작업은 직접 처리 |
| **에이전트 호환** | 범용 — 파일 읽기·쓰기 작업 중심으로 모든 에이전트 처리 가능 |

""",
"oodesign": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 디자인 시스템 통합 스킬 — 토큰·컴포넌트·테마 생성 및 AI 디자인·코드 변환 |
| **하는 것** | 디자인 토큰 생성, 컴포넌트 파일 생성, 테마 관리, URL 레퍼런스 분석, 디자인→코드 변환 |
| **하지 않는 것** | 앱 코드 구현(→oodev), 문서 작성(→oodoc), 스크린샷 캡처(→oocapture) |
| **참조 범위** | 현재 프로젝트 내부 파일 + 지정 URL 레퍼런스 / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `07_designsystem/` 하위 파일 (03_vars/, 04_components/, 05_themes/ 등) |
| **실행 레벨** | [반자동] — 생성 계획 확인 후 실행 |
| **에이전트 호환** | 범용 — URL 레퍼런스 분석 시 웹 접근 필요 |

""",
"ootutorial": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | OAIS 환경(스킬·명령어·플러그인)에 대한 튜토리얼 문서 생성·관리 |
| **하는 것** | `.claude/tutorial/` 튜토리얼 MD 파일 생성, 기존 튜토리얼 현행화 |
| **하지 않는 것** | API 문서(→oodoc), 코드 수정(→oodev) |
| **참조 범위** | 현재 프로젝트 내부 파일만 (스킬 문서, 가이드) / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `.claude/tutorial/*.md` |
| **실행 레벨** | [반자동] — 튜토리얼 주제 확인 후 작성 |
| **에이전트 호환** | 범용 — 파일 읽기·쓰기 작업 중심으로 모든 에이전트 처리 가능 |

""",
"oolib": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | oo 공통 모듈(oo/) 문제 발견→기록→수정 2단계 워크플로우 |
| **하는 것** | oo 모듈 분석, 이슈 d{SP}0004 등록, 수정 후 해결 이동 |
| **하지 않는 것** | 서브프로젝트 코드 수정(→oofix), DB 수정(→oodb), 성능 최적화(→ooopti) |
| **참조 범위** | 현재 프로젝트 내부 `oo/` 모듈만 / 외부 라이브러리 자동 포함 안 함 |
| **수정 대상** | `oo/*.py`, `d{SP}0004_todo.md`, `d{SP}0010_history.md` |
| **실행 레벨** | [자동] — Phase 1(분석) → Phase 2(수정) 자동 실행 |
| **에이전트 호환** | Claude Code 권장 — `uv run` 환경 자동 사용 / 다른 에이전트: 분석·수정 단계를 수동 실행 |

""",
"oodb": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | DB 문제 발견→기록→수정 3단계 워크플로우 (Python/Flutter 지원) |
| **하는 것** | 스키마 검증, 마이그레이션 생성, DB 이슈 수정, d{SP}0004 등록 |
| **하지 않는 것** | 코드 로직 수정(→oofix), oo 모듈 수정(→oolib), 의존성 관리(→oouv) |
| **참조 범위** | 현재 프로젝트 내부 파일만 (DB 파일, 마이그레이션) / 외부 DB 서버 자동 접속 안 함 |
| **수정 대상** | DB 스키마 파일, 마이그레이션 파일, `d{SP}0004_todo.md` |
| **실행 레벨** | [반자동] — 스키마 변경 확인 후 실행 |
| **에이전트 호환** | 범용 — 파일 기반 마이그레이션 중심으로 모든 에이전트 처리 가능 |

""",
"oodeep": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | PyTorch 딥러닝 코드 GPU 효율성 모니터링 및 자동 최적화 반복 사이클 |
| **하는 것** | 학습 실행, GPU 효율 모니터링, 비효율 감지 시 코드 최적화, 최대 5회 반복 |
| **하지 않는 것** | 일반 코드 최적화(→ooopti), 의존성 관리(→oouv), 모델 평가(→ooresearch) |
| **참조 범위** | 현재 프로젝트 내부 파일만 (PyTorch 코드) / 외부 데이터셋 자동 다운로드 안 함 |
| **수정 대상** | PyTorch 학습 코드 파일 |
| **실행 레벨** | [자동] — 실행→모니터링→판단→최적화→재실행 자동 사이클 |
| **에이전트 호환** | Claude Code 권장 — GPU 환경에서 `uv run` 자동 실행 / 다른 에이전트: GPU 모니터링 결과를 수동 확인 후 최적화 적용 |

""",
"ooopti": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 알고리즘 분석 및 코드 성능 최적화 (시간복잡도·메모리·병목 제거) |
| **하는 것** | 성능 프로파일링, 알고리즘 개선, 병목 분석, 최적화 코드 적용 |
| **하지 않는 것** | GPU 딥러닝 최적화(→oodeep), 코드 에러 수정(→oofix), 코드 품질 체크(→oocheck) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 벤치마크 자동 포함 안 함 |
| **수정 대상** | 최적화 대상 코드 파일 |
| **실행 레벨** | [반자동] — 프로파일링 결과 확인 후 최적화 적용 |
| **에이전트 호환** | 범용 — 코드 분석·수정 중심으로 모든 에이전트 처리 가능 |

""",
"oonote": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 날짜/시간 기반 연구노트를 d{SP}0020_연구노트.md에 작성·조회 |
| **하는 것** | 연구노트 항목 추가(타임스탬프 자동), 조회, 요약 |
| **하지 않는 것** | 옵시디언 노트(→oosidi), 이력 관리(→oohistory), 연구 수행(→ooresearch) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 프로젝트 자동 포함 안 함 |
| **수정 대상** | `d{SP}0020_연구노트.md` |
| **실행 레벨** | [자동] — 입력 내용 즉시 파일에 기록 |
| **에이전트 호환** | 범용 — 파일 읽기·쓰기 작업 중심으로 모든 에이전트 처리 가능 |

""",
"oosidi": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 01_obsidian/ 볼트의 마크다운 문서 생성·편집·검색·구조 관리 |
| **하는 것** | 옵시디언 노트 CRUD, 태그·링크 관리, 볼트 구조 탐색 |
| **하지 않는 것** | 연구노트(→oonote), 프로젝트 문서(→oodoc), 소개서 작성(→oosidi [소개서]) |
| **참조 범위** | `01_obsidian/` 볼트 내부 파일만 / 외부 볼트 자동 포함 안 함 |
| **수정 대상** | `01_obsidian/**/*.md` |
| **실행 레벨** | [반자동] — 노트 생성/수정 내용 확인 후 실행 |
| **에이전트 호환** | 범용 — 마크다운 파일 작업 중심으로 모든 에이전트 처리 가능 |

""",
"oorun": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | TDD 기반 프로젝트 자율 실행 (Builder) — 빌드·테스트·배포 자동화 |
| **하는 것** | 빌드 스크립트 실행, TDD 사이클 자율 진행, 실행 환경 설정 |
| **하지 않는 것** | 코드 구현(→oodev), 정적 분석(→oocheck), 이슈 수정(→oofix) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 서버 자동 배포 안 함 |
| **수정 대상** | 빌드 산출물, 실행 환경 설정 |
| **실행 레벨** | [자동] — 빌드·테스트 자동 실행 |
| **에이전트 호환** | Claude Code 권장 — `uv run` 기반 자동 실행 / 다른 에이전트: 빌드 명령을 수동 실행 |

""",
"oouv": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | UV 기반 Python 의존성 관리 (추가·업데이트·취약점 검사) |
| **하는 것** | uv add/remove/update, 취약점 스캔, pyproject.toml/uv.lock 관리 |
| **하지 않는 것** | 환경 전체 점검(→ooenv), 코드 수정(→oofix), GPU 설치(→oodeep) |
| **참조 범위** | 현재 프로젝트 `pyproject.toml`, `uv.lock` / 외부 레지스트리 접속 가능 |
| **수정 대상** | `pyproject.toml`, `uv.lock` |
| **실행 레벨** | [반자동] — 변경 대상 패키지 확인 후 실행 |
| **에이전트 호환** | Claude Code 권장 — `uv` CLI 자동 실행 / 다른 에이전트: `uv add`, `uv sync` 등 수동 실행 |

""",
"oocontext": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 현재 세션의 서브프로젝트(SP) 컨텍스트 설정 — 이후 모든 스킬이 해당 SP 기준으로 동작 |
| **하는 것** | SP 번호 지정, SP별 문서 경로 규칙 설정, 현재 컨텍스트 표시 |
| **하지 않는 것** | 파일 생성·수정, 코드 실행, 외부 연동 |
| **참조 범위** | 세션 내 컨텍스트 설정만 / SP 목록은 `d0001_prd.md §2.1` 참조 |
| **수정 대상** | 없음 (세션 상태만 변경) |
| **실행 레벨** | [수동] — 즉시 적용, 파일 수정 없음 |
| **에이전트 호환** | 범용 — SP 번호를 환경 변수나 인자로 전달하는 방식으로 모든 에이전트 활용 가능 |

""",
"oonext": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | PRD·Plan·Todo 문서를 분석하여 다음에 진행할 작업 우선순위 추천 |
| **하는 것** | 미완료 Feature/이슈 분석, 우선순위 정렬, 다음 작업 추천 목록 출력 |
| **하지 않는 것** | 작업 실행(→ooflow/oodev), 이슈 수정(→oofix), 문서 수정(→oodoc) |
| **참조 범위** | 현재 프로젝트 내부 문서 파일만 (d{SP}0001~0004) / 외부 시스템 자동 포함 안 함 |
| **수정 대상** | 없음 (읽기·분석·출력만) |
| **실행 레벨** | [수동] — 분석 결과 출력만, 파일 수정 없음 |
| **에이전트 호환** | 범용 — 파일 읽기 기반 분석으로 모든 에이전트 처리 가능 |

""",
"oonow": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 직전 사용자 입력과 현재 작업 컨텍스트를 요약 표시 |
| **하는 것** | 현재 세션의 직전 요청 내용 + 컨텍스트 요약 출력 |
| **하지 않는 것** | 파일 수정, 작업 실행, 이전 세션 정보 조회(→ooprevious) |
| **참조 범위** | 현재 세션 내 컨텍스트만 / 외부 파일·시스템 자동 포함 안 함 |
| **수정 대상** | 없음 (출력만) |
| **실행 레벨** | [수동] — 세션 상태 표시만 |
| **에이전트 호환** | Claude Code 전용 — 세션 컨텍스트 접근 필요 / 다른 에이전트: 현재 대화 내용 요약으로 대체 가능 |

""",
"ooprevious": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 현재 요청 직전의 사용자 요청 내용과 결과 표시 |
| **하는 것** | 직전 요청-응답 단위의 내용과 결과 요약 출력 |
| **하지 않는 것** | 파일 수정, 작업 실행, 현재 상황 표시(→oonow) |
| **참조 범위** | 현재 세션 내 히스토리만 / 외부 파일·시스템 자동 포함 안 함 |
| **수정 대상** | 없음 (출력만) |
| **실행 레벨** | [수동] — 세션 히스토리 읽기만 |
| **에이전트 호환** | Claude Code 전용 — 세션 히스토리 접근 필요 / 다른 에이전트: 대화 히스토리 스크롤로 대체 |

""",
"ootoken": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 토큰 사용량 모니터링 및 리밋 도달 예측 표시 |
| **하는 것** | 5시간·주간 사용률 표시, 리밋 예측, 모델 헤더 표시 |
| **하지 않는 것** | 토큰 절약 최적화, 모델 변경, 세션 종료(→oostop) |
| **참조 범위** | OMC HUD 캐시 파일 (`~/.claude/plugins/.usage-cache.json`) |
| **수정 대상** | 없음 (읽기·출력만) |
| **실행 레벨** | [수동] — 캐시 읽기 후 표시만 |
| **에이전트 호환** | Claude Code 전용 — OMC HUD 캐시 의존 / 다른 에이전트: 해당 캐시 파일이 없으면 동작 안 함 |

""",
"oowork": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 기술수요조사·제안서·보고서 등 반복 문서 작업 프로세스를 정의하고 순차 실행 |
| **하는 것** | 워크플로우 프로세스 등록, 단계별 문서 생성, 비텍스트 문서 마크다운 변환 |
| **하지 않는 것** | 코드 개발(→oodev), 논문 작성(→oosota), PPT 생성(→ooppt) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 시스템 자동 포함 안 함 |
| **수정 대상** | 워크플로우 정의 파일, 문서 산출물 |
| **실행 레벨** | [반자동] — 각 단계 완료 확인 후 다음 단계 진행 |
| **에이전트 호환** | 범용 — 문서 생성 중심으로 모든 에이전트 처리 가능 |

""",
"oocapture": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | Flutter/Web 앱을 웹 빌드 후 Playwright로 화면 캡처 (PNG + HTML 저장) |
| **하는 것** | Flutter 웹 빌드, Playwright 브라우저 실행, 스크린샷·HTML 저장 |
| **하지 않는 것** | 코드 수정(→oodev), E2E 테스트(→ootest), 앱 배포 |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 서버 자동 배포 안 함 |
| **수정 대상** | 캡처 저장 폴더 내 PNG, HTML 파일 |
| **실행 레벨** | [자동] — 빌드→실행→캡처 자동 처리 |
| **에이전트 호환** | Claude Code 권장 — Playwright MCP 서버 사용 / 다른 에이전트: Playwright 직접 실행으로 대체 |

""",
"ooskill": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | oo 스킬 파일들의 서브에이전트·명령어 활용 최적화 검증 및 개선 |
| **하는 것** | SKILL.md 검증, 서브에이전트 위임 최적화, 스킬 문서 이력 자동 추가 |
| **하지 않는 것** | 코드 구현(→oodev), 환경 점검(→ooenv), 스킬 직접 실행 |
| **참조 범위** | `.claude/skills/oo*/SKILL.md` 파일들 / 외부 스킬 저장소 자동 포함 안 함 |
| **수정 대상** | `.claude/skills/oo*/SKILL.md` |
| **실행 레벨** | [반자동] — 검증 결과 확인 후 수정 적용 |
| **에이전트 호환** | 범용 — 파일 읽기·쓰기 작업 중심으로 모든 에이전트 처리 가능 |

""",
"oosync": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | OAIS 프로젝트와 3_code/ 독립 프로젝트 간 vibe 파일 동기화 |
| **하는 것** | .claude/ 파일 비교·복사, .claudeignore 적용, 동기화 결과 보고 |
| **하지 않는 것** | 코드 수정(→oodev), Git 커밋(→oocommit), 환경 점검(→ooenv) |
| **참조 범위** | 현재 프로젝트 + `OAIS_SYNC_TARGET` 환경변수 경로 (기본: `3_code/`) |
| **수정 대상** | 대상 프로젝트의 `.claude/` 관련 파일 |
| **실행 레벨** | [반자동] — 동기화 대상 확인 후 실행 |
| **에이전트 호환** | 범용 — 파일 복사 작업 중심 / `OAIS_SYNC_TARGET` 환경변수 필요 |

""",
"ooppt": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 대상 MD 문서를 PPT 발표용 슬라이드 노트 형식 MD로 변환 |
| **하는 것** | MD→슬라이드 노트 MD 변환, 발표 분량 조절, 섹션별 노트 구성 |
| **하지 않는 것** | PPTX 파일 직접 생성(→document-skills:pptx), Word 변환(→ooword), 리포트 생성(→ooreport) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 프레젠테이션 서비스 자동 연동 안 함 |
| **수정 대상** | 입력 MD 기반 새 `*_노트.md` 파일 생성 |
| **실행 레벨** | [반자동] — 변환 구조 확인 후 생성 |
| **에이전트 호환** | 범용 — 파일 읽기·쓰기 작업 중심으로 모든 에이전트 처리 가능 |

""",
"ooword": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | Markdown을 Word(.docx) 문서로 변환 |
| **하는 것** | MD→docx 변환, 스타일 적용, 견적서 생성 |
| **하지 않는 것** | PPT 생성(→ooppt), PDF 변환(→oopdf), HWP 생성(→oohwp) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 문서 서비스 자동 연동 안 함 |
| **수정 대상** | 출력 `*.docx` 파일 |
| **실행 레벨** | [자동] — 변환 자동 실행 |
| **에이전트 호환** | Claude Code 권장 — python-docx/pandoc `uv run` 자동 실행 / 다른 에이전트: `pandoc input.md -o output.docx` 수동 실행 |

""",
"oobook": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 도서 내용을 구조화된 서머리 MD 문서로 생성 |
| **하는 것** | 도서 내용 분석, 챕터별 요약, 핵심 개념 정리, 서머리 MD 생성 |
| **하지 않는 것** | 유튜브 콘텐츠 처리(→ooscrap), 논문 요약(→oopaper), 리포트 생성(→ooreport) |
| **참조 범위** | 현재 프로젝트 내부 파일만 (도서 텍스트) / 외부 전자책 서비스 자동 접속 안 함 |
| **수정 대상** | 서머리 MD 파일 |
| **실행 레벨** | [반자동] — 서머리 구조 확인 후 생성 |
| **에이전트 호환** | 범용 — 텍스트 분석·요약 중심으로 모든 에이전트 처리 가능 |

""",
"oopaper": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 학술 논문·국내 보고서 통합 문헌 관리 (수집·분류·분석·번역) |
| **하는 것** | 논문 수집, 분류 정리, 영문 전문·한글 번역 생성, 03_paper/ 관리 |
| **하지 않는 것** | 논문 작성(→oosota), 서베이 생성(→oosurvey), 연구 수행(→ooresearch) |
| **참조 범위** | 현재 프로젝트 `03_paper/` + 웹 검색(논문 수집 시) |
| **수정 대상** | `03_paper/**/*.md`, `d30004_todo.md` |
| **실행 레벨** | [반자동] — 수집 대상 확인 후 실행 |
| **에이전트 호환** | Claude Code 권장 — academic-researcher 서브에이전트 자동 배치 / 다른 에이전트: 웹 검색 후 수동으로 파일 생성 |

""",
"ooreport": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 데이터 소스 기반 리포트 생성 (MD/PDF/PPTX 다중 포맷) |
| **하는 것** | 데이터 로드, 템플릿 렌더링, MD·PDF·PPTX 포맷 출력 |
| **하지 않는 것** | Word 문서(→ooword), 학술 논문(→oosota), PPT 노트(→ooppt) |
| **참조 범위** | 현재 프로젝트 내부 데이터 파일·템플릿 / 외부 데이터 서비스 자동 연동 안 함 |
| **수정 대상** | 리포트 출력 파일 (MD/PDF/PPTX) |
| **실행 레벨** | [반자동] — 데이터 소스·포맷 확인 후 생성 |
| **에이전트 호환** | Claude Code 권장 — `uv run` 기반 변환 라이브러리 자동 실행 / 다른 에이전트: pandoc·weasyprint 수동 실행 |

""",
"oopdf": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | Markdown→PDF 변환 및 PDF→이미지 변환 |
| **하는 것** | MD→PDF 변환(weasyprint/pandoc), PDF→PNG 변환(PyMuPDF) |
| **하지 않는 것** | Word 변환(→ooword), PPT 생성(→ooppt), 리포트 생성(→ooreport) |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 PDF 서비스 자동 연동 안 함 |
| **수정 대상** | 출력 `*.pdf`, `*.png` 파일 |
| **실행 레벨** | [자동] — 변환 자동 실행 |
| **에이전트 호환** | Claude Code 권장 — weasyprint/pandoc/PyMuPDF `uv run` 자동 실행 / 다른 에이전트: 해당 CLI 도구 수동 실행 필요 |

""",
"ooresearch": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | SOTA 비교 기준으로 특정 분야 연구를 체계적으로 수행 (복수 에이전트 병렬) |
| **하는 것** | 03_paper/ 기존 논문 활용, 부족 자료 oopaper 자동 수집, 다각도 연구 분석 |
| **하지 않는 것** | 논문 작성(→oosota), 서베이 생성(→oosurvey), 문헌 수집만(→oopaper) |
| **참조 범위** | 현재 프로젝트 `03_paper/` + 웹 검색 / 외부 DB 자동 크롤링 안 함 |
| **수정 대상** | 연구 결과 문서 파일 |
| **실행 레벨** | [자동] — 복수 에이전트 병렬 연구 자동 실행 |
| **에이전트 호환** | Claude Code 권장 — 병렬 서브에이전트 자동 배치 / 다른 에이전트: 연구 단계별 순차 실행 |

""",
"oosota": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 실험 결과 기반 SOTA급 저널 논문 초안 작성 (IEEE/Elsevier/Springer 포맷) |
| **하는 것** | 실험 데이터 분석, 논문 구조 생성, Abstract·Introduction·Conclusion 작성 |
| **하지 않는 것** | 연구 수행(→ooresearch), 문헌 수집(→oopaper), 서베이 작성(→oosurvey) |
| **참조 범위** | 현재 프로젝트 내부 파일 (실험 데이터) + 웹 참고문헌 검색 |
| **수정 대상** | 논문 초안 MD 파일 |
| **실행 레벨** | [반자동] — 실험 데이터 확인 후 작성 |
| **에이전트 호환** | Claude Code 권장 — academic-researcher 에이전트 연동 / 다른 에이전트: 연구 컨텍스트를 직접 제공하여 논문 작성 |

""",
"oosurvey": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 논문 서베이 및 선행연구 체계적 정리 |
| **하는 것** | 논문 분류·비교표 생성, 연구 동향 분석, 서베이 문서 작성 |
| **하지 않는 것** | 새 논문 작성(→oosota), 문헌 수집(→oopaper), 연구 수행(→ooresearch) |
| **참조 범위** | 현재 프로젝트 `03_paper/` 내부 파일 + 웹 검색 보조 |
| **수정 대상** | 서베이 결과 MD 파일 |
| **실행 레벨** | [반자동] — 분석 범위 확인 후 생성 |
| **에이전트 호환** | 범용 — 텍스트 분석·문서 생성 중심으로 모든 에이전트 처리 가능 |

""",
"ooscrap": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 유튜브 STT·다운로드 및 웹 기사 전문 스크래핑 통합 스킬 |
| **하는 것** | 유튜브 URL→STT/자막 추출, 웹 기사 전문 스크래핑, 결과 MD 저장 |
| **하지 않는 것** | 도서 서머리(→oobook), 논문 수집(→oopaper), 데이터 분석(→ooresearch) |
| **참조 범위** | 지정 URL(유튜브/일반 웹) + 현재 프로젝트 저장 경로 |
| **수정 대상** | 스크래핑 결과 MD 파일, 다운로드 파일 |
| **실행 레벨** | [자동] — URL 입력 후 자동 처리 |
| **에이전트 호환** | Claude Code 권장 — yt-dlp·whisper `uv run` 자동 실행 / 다른 에이전트: yt-dlp 수동 실행 후 결과를 직접 처리 |

""",
"oohwp": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 한글(HWPX) 문서를 XML-first 방식으로 생성·편집·읽기 |
| **하는 것** | HWPX 파일 생성/편집, 공문 작성, HWP→MD 읽기 |
| **하지 않는 것** | Word 문서(→ooword), PDF 변환(→oopdf), 한글 앱 직접 제어 |
| **참조 범위** | 현재 프로젝트 내부 파일만 / 외부 한글 서버 자동 연결 안 함 |
| **수정 대상** | `*.hwpx` 파일 |
| **실행 레벨** | [반자동] — 문서 구조 확인 후 생성 |
| **에이전트 호환** | Claude Code 권장 — `uv run --with lxml` 자동 실행 / 다른 에이전트: lxml 설치 후 수동 스크립트 실행 |

""",
"oostart": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 세션 시작 시 환경 점검·문서 동기화·체크리스트 실행 |
| **하는 것** | 서브프로젝트 스캔, common_guide 로드, 문서 상태 점검, oocheck 연동 안내 |
| **하지 않는 것** | 코드 수정(→oodev), 환경 설치(→ooenv), 파일 생성(→oodoc) |
| **참조 범위** | 현재 프로젝트 내부 문서 파일 + 환경변수 / 외부 시스템 자동 연결 안 함 |
| **수정 대상** | 없음 (점검·보고만) |
| **실행 레벨** | [자동] — 세션 시작 시 `uv run python ...` 자동 실행 |
| **에이전트 호환** | Claude Code 권장 — `uv run` 스크립트 실행 / 다른 에이전트: 문서 상태를 수동으로 확인하거나 점검 항목을 직접 실행 |

""",
"oostop": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | 세션 종료 전 미완료 항목 정리·요약·이력 기록 |
| **하는 것** | 미완료 작업 목록 표시, 이력 정리 안내, 세션 요약 출력 |
| **하지 않는 것** | 파일 강제 저장(자동 저장됨), git push, 세션 외부 상태 변경 |
| **참조 범위** | 현재 프로젝트 내부 문서 파일 / 외부 시스템 자동 연결 안 함 |
| **수정 대상** | `d{SP}0010_history.md` (세션 요약 기록, 선택) |
| **실행 레벨** | [반자동] — 정리 항목 확인 후 종료 |
| **에이전트 호환** | 범용 — 파일 읽기·요약 중심으로 모든 에이전트 처리 가능 |

""",
"oohelp": """\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | oo 스킬·명령어 목록 및 도움말 표시 |
| **하는 것** | 스킬 목록 출력, 명령어 설명 표시, 카테고리별 안내 |
| **하지 않는 것** | 스킬 실행, 파일 수정, 환경 변경 |
| **참조 범위** | `.claude/skills/` 스킬 파일 목록 / 외부 문서 자동 포함 안 함 |
| **수정 대상** | 없음 (출력만) |
| **실행 레벨** | [수동] — 도움말 출력만 |
| **에이전트 호환** | 범용 — 스킬 목록 파일 읽기 중심으로 모든 에이전트 처리 가능 |

""",
}

# alias 스킬: 간략 섹션
ALIAS_SECTIONS = {
    "ooc":  ("oocontext", "서브프로젝트 컨텍스트 전환"),
    "ood":  ("oodev",     "TDD 기반 개발"),
    "oof":  ("oofeature", "기능별 상세 문서 생명주기 관리"),
    "ook":  ("oocheck",   "코드 품질 체크"),
    "oos":  ("oostart",   "세션 시작 워크플로우"),
}

for alias, (target, role) in ALIAS_SECTIONS.items():
    SECTIONS[alias] = f"""\
## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | `{target}` 스킬의 약어 alias — {role} |
| **하는 것** | `{target}` 스킬과 동일 |
| **하지 않는 것** | alias 스킬 자체는 추가 로직 없음 |
| **에이전트 호환** | 범용 — `{target}`을 직접 호출하는 것으로 대체 가능 |

"""


def insert_section(skill_name, section_text):
    skill_file = os.path.join(BASE, skill_name, "SKILL.md")
    if not os.path.exists(skill_file):
        print(f"[SKIP] {skill_file} not found")
        return

    with open(skill_file, "r", encoding="utf-8") as f:
        content = f.read()

    # 이미 삽입된 경우 스킵
    if "## 0. 스킬 요약" in content:
        print(f"[SKIP] {skill_name}: already has section")
        return

    # 첫 번째 ## 헤딩 위치 탐색
    match = re.search(r"^## ", content, re.MULTILINE)
    if match:
        pos = match.start()
        new_content = content[:pos] + section_text + content[pos:]
    else:
        new_content = content + "\n\n" + section_text

    with open(skill_file, "w", encoding="utf-8") as f:
        f.write(new_content)
    print(f"[OK] {skill_name}")


if __name__ == "__main__":
    ok = skip = 0
    for skill, section in SECTIONS.items():
        skill_file = os.path.join(BASE, skill, "SKILL.md")
        if not os.path.exists(skill_file):
            print(f"[SKIP] {skill}: file not found")
            skip += 1
            continue
        with open(skill_file, "r", encoding="utf-8") as f:
            content = f.read()
        if "## 0. 스킬 요약" in content:
            print(f"[SKIP] {skill}: already updated")
            skip += 1
            continue
        match = re.search(r"^## ", content, re.MULTILINE)
        if match:
            pos = match.start()
            new_content = content[:pos] + section + content[pos:]
        else:
            new_content = content + "\n\n" + section
        with open(skill_file, "w", encoding="utf-8") as f:
            f.write(new_content)
        print(f"[OK] {skill}")
        ok += 1

    print(f"\n완료: {ok}개 업데이트, {skip}개 스킵")
