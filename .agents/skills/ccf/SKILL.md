---
name: ccf
description: "**관련 명령어**: analyze, implement (`.claude/commands/sc/`)"
---

<!-- ccporting:generated-from-upstream -->
<!-- 원본 스킬은 upstream/ 폴더에 보관된다. -->

이 스킬은 **`ccfeature`의 약어 alias**입니다.

전달된 인자(`$ARGUMENTS`)를 그대로 유지하여 **즉시 `ccfeature` 스킬을 실행**하세요.
Skill 도구로 `ccfeature`를 호출하고 `$ARGUMENTS`를 args로 전달합니다.

> **관련 명령어**: analyze, implement (`.claude/commands/sc/`)


## 0. 스킬 요약

| 항목 | 내용 |
|------|------|
| **핵심 역할** | `ccfeature` 스킬의 약어 alias — 기능별 상세 문서 생명주기 관리 |
| **하는 것** | `ccfeature` 스킬과 동일 |
| **하지 않는 것** | alias 스킬 자체는 추가 로직 없음 |
| **에이전트 호환** | 범용 — `ccfeature`을 직접 호출하는 것으로 대체 가능 |

