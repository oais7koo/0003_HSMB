# {기능명} 상세기획

> 문서번호: {dXXXX} | 단계: 기획 | SP: {SPNN} | 생성일: {YYYY-MM-DD}
> 연결 Feature: {F_ID} | plan.md 참조: {페이지번호_기능명}
> 프레임워크: Streamlit (`04_backoffice/`)

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| v01 | {YYYY-MM-DD} | 초기 작성 |

---

## 1. 문서 관리

| 항목 | 내용 |
|------|------|
| 문서번호 | `{dXXXX}` |
| 대상 기능 | `{F_ID} {기능명}` |
| 원본 페이지 | `04_backoffice/pages/{N_NN_기능명.py}` (없으면 삭제) |
| oais 함수 | `oais/{module}.py {function}()` (없으면 삭제) |
| URL | `/:N_NN_기능명` |
| 버전 | v01 |
| 작성일 | `{YYYY-MM-DD}` |
| 관련 문서 | (없으면 삭제) |

---

## 2. 기능 개요

> 이 페이지가 무엇을 하는지 1~3문장으로 설명.
> 주요 관리 대상(데이터/설정/사용자 등)과 접근 권한 요약을 포함할 것.

---

## 3. 요구사항

| ID | 요구사항 | 우선순위 | 출처 |
|----|---------|---------|------|
| R01 | | Must | PRD |
| R02 | | Must | PRD |
| R03 | | Should | PRD |
| R0N | 접근 제한 탭 admin 전용 접근 제어 (비admin → 차단 메시지) | Must | 설계 결정 |

> **우선순위**: Must(필수) / Should(권장) / Could(선택)
> admin 전용 기능이 있으면 RBAC 요구사항을 Must로 반드시 포함

---

## 4. 입출력 정의

### 4.1 화면 구성

#### 레이아웃 (Streamlit 컴포넌트 명세) — `{N_NN_기능명.py}`

```
st.title("{페이지 제목}")
tab1, tab2 = st.tabs(["{탭1명}", "{탭2명}"])
```

> **탭 설계 원칙**:
> - 조회/현황 탭: 전체 계정 접근 가능 → 앞 배치
> - 설정/관리 탭: admin 전용 → 마지막 배치
> - 탭이 불필요한 단순 페이지는 탭 구조 생략 가능

| 탭 | 접근 권한 | 내용 요약 |
|----|----------|----------|
| {탭1명} | 전체 계정 | {내용 요약} |
| {탭2명} | **admin 전용** | {내용 요약} |

---

**[탭1] {탭1명}** — 전체 계정 접근 가능

| 위젯 | 타입 | 설명 |
|------|------|------|
| {항목명} | `st.metric` / `st.dataframe` / `st.info` | {설명} |
| {항목명} | `st.selectbox` / `st.text_input` | {설명} |

---

**[탭N] {설정탭명}** — admin 전용

```python
# 비admin 접근 차단
if current_user.role != "admin":
    st.warning("🔒 시스템 관리자 계정만 접근할 수 있습니다.")
    st.stop()
```

**섹션 A — {섹션명}**

| 위젯 | 타입 | 옵션 / 기본값 | 동작 |
|------|------|--------------|------|
| {항목명} | `st.selectbox` | {옵션 목록} | {동작 설명} |
| {항목명} | `st.text_input` / `st.text_area` | {기본값} | {동작 설명} |
| {항목명} | `st.number_input(min={N}, max={N})` | {기본값} | — |
| 저장 버튼 | `st.button` | — | DB 저장 후 `st.success` / `st.error` |

**섹션 B — {섹션명}** _(필요 시 추가, 불필요 시 삭제)_

| 위젯 | 타입 | 설명 |
|------|------|------|
| {항목명} | `st.dataframe` | {설명} |
| 삭제/처리 버튼 | `st.button` | 선택 항목 처리 → `st.success` / `st.error` |

---

#### 항목 상세

> 탭/섹션별 입력 항목의 유효성·제약을 정의. 위젯 명세에서 다루지 못한 세부 규칙 기술.

**{섹션명}**

| 항목 | 타입 | 설명 | 필수 |
|------|------|------|:----:|
| {항목명} | selectbox / text_input / number_input | {설명} | O / - |

### 4.2 저장 출력

| 항목 | 저장 위치 | 비고 |
|------|----------|------|
| {민감 설정값} | `.env` 파일 (`{ENV_KEY_NAME}`) | 화면 재로드 시 마스킹, 서버 측 전용 |
| {일반 설정값} | DB `{테이블명}` | 즉시 적용 가능 |
| {운영 데이터} | DB `{테이블명}` | {비고} |

**{테이블명} 테이블 구조 (신규):** _(기존 테이블 사용 시 삭제)_

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | INTEGER PK AUTOINCREMENT | |
| {컬럼명} | TEXT / INTEGER / REAL / DATETIME | {설명} |
| created_at | DATETIME | 생성 시각 |
| updated_at | DATETIME | 최종 수정 시각 |

---

## 5. 제약조건 / 예외처리

| 상황 | 처리 방식 |
|------|----------|
| 필수 입력값 미입력 | `st.warning` 표시, 저장 차단 |
| 저장 실패 | `st.error` 메시지 + 이전 값 유지 |
| 접근 제한 탭 비admin 접근 | `st.warning("🔒 시스템 관리자 계정만 접근할 수 있습니다.")` + `st.stop()` |
| {기능별 예외 상황} | {처리 방식} |

---

## 6. 관련 Feature (plan.md 연결)

- 연결 Feature: `{F_ID}` — {Feature명} (SP00 PRD §{섹션})
- 연동 페이지: `{dXXXX}` — {페이지명} ({연동 목적})
- 앱 API: `{dXXXX}` — SP05 PRD §{섹션} `{엔드포인트}` _(앱 연동 없으면 생략)_

---

## 7. 참고 자료

- PRD(공통): `00_doc/sp00/d0001_prd.md` §{섹션} {Feature명}
- PRD(SP04): `00_doc/sp04/d40001_prd.md` §{섹션} {페이지번호_기능명}
- 연계 문서: `{dXXXX_상세기획_연계기능.md}` _(없으면 생략)_

---

## 8. 이슈

| 날짜 | 내용 | 상태 |
|------|------|------|
